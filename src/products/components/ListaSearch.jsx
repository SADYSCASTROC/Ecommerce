
export const ListaSearch = ({resultado}) => {
  return (
    <div>
                {/* {resultado.map(resul => (
                    <div key={resul.id}>{resul.title}</div>
                ))}
             */}
    </div>
  )
}
